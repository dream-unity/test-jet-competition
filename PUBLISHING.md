# Publishing

The validated static build is committed to `main` and mirrored to `gh-pages`. The official Pages workflow validates the relational engine, renderer and deployment structure before publishing.
