name: Verify live Apex One-Race v5 deployment

on:
  push:
    branches: [main]
    paths-ignore:
      - 'CI_STATUS.json'
      - 'DEPLOYMENT_STATUS.json'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apex-v5-live-deployment-verification
  cancel-in-progress: true

jobs:
  verify-live:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: main
          fetch-depth: 0

      - name: Wait for the live one-race fighter runtime to exactly match tested source
        shell: bash
        env:
          PAGE_URL: https://dream-unity.github.io/test-jet-competition
        run: |
          set -euo pipefail
          files=(
            index.html
            fighter-v4.css
            single-race-v5.css
            src/math3d.js
            src/renderer-v4.js
            src/fighter-audio.js
            src/fighter-input.js
            src/fighter-flight.js
            src/course3d.js
            src/fighter-visuals.js
            src/relational-racing.js
            src/race-progression.js
            src/fighter-game.js
            src/single-race-v5.js
          )
          mkdir -p /tmp/apex-live/src
          matched='false'
          for attempt in $(seq 1 40); do
            matched='true'
            for path in "${files[@]}"; do
              if ! curl --fail --location --silent --show-error \
                --retry 3 --retry-all-errors --retry-delay 2 \
                "$PAGE_URL/$path?source=${GITHUB_SHA}" --output "/tmp/apex-live/$path"; then
                matched='false'
                break
              fi
              if ! cmp --silent "$path" "/tmp/apex-live/$path"; then
                matched='false'
                break
              fi
            done
            if [ "$matched" = 'true' ]; then break; fi
            echo "Deployment has not propagated yet (attempt $attempt/40)."
            sleep 5
          done
          test "$matched" = 'true'
          grep -q '<title>Apex Relational Racing' /tmp/apex-live/index.html
          grep -q 'src/single-race-v5.js' /tmp/apex-live/index.html
          grep -q 'ONE CONTINUOUS COMPETITION' /tmp/apex-live/index.html
          grep -q 'SINGLE_RACE_CONFIG' /tmp/apex-live/src/single-race-v5.js
          grep -q 'updateForgivingProgress' /tmp/apex-live/src/single-race-v5.js
          grep -q 'composeFlightAxes' /tmp/apex-live/src/fighter-input.js
          grep -q 'maximumSpeed' /tmp/apex-live/src/fighter-flight.js
          grep -q 'level: 10' /tmp/apex-live/src/race-progression.js
          grep -q 'vortex-convergence' /tmp/apex-live/src/relational-racing.js
          if grep -Eqi 'class="mode-card|data-mode=|MODE SELECT|src/main\.js|world-racing\.js|correctIndex|candidate\.pattern|renderPattern\(' \
             /tmp/apex-live/index.html /tmp/apex-live/src/single-race-v5.js; then
            echo 'Live surface unexpectedly contains rejected multi-mode or legacy puzzle architecture.' >&2
            exit 1
          fi
          sha256sum /tmp/apex-live/index.html /tmp/apex-live/fighter-v4.css \
            /tmp/apex-live/single-race-v5.css /tmp/apex-live/src/*.js > /tmp/apex-live-checksums.txt

      - name: Commit verified deployment receipt
        shell: bash
        env:
          SOURCE_SHA: ${{ github.sha }}
          PAGE_URL: https://dream-unity.github.io/test-jet-competition/
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from datetime import datetime, timezone
          from pathlib import Path
          import json
          import os

          receipt = {
              'game': 'Apex Relational Racing',
              'deployment': 'verified-live',
              'architecture': 'one-race-fluid-fighter-v5',
              'page_url': os.environ['PAGE_URL'],
              'source_sha': os.environ['SOURCE_SHA'],
              'files_compared': [
                  'index.html', 'fighter-v4.css', 'single-race-v5.css',
                  'src/math3d.js', 'src/renderer-v4.js', 'src/fighter-audio.js',
                  'src/fighter-input.js', 'src/fighter-flight.js', 'src/course3d.js',
                  'src/fighter-visuals.js', 'src/relational-racing.js',
                  'src/race-progression.js', 'src/fighter-game.js', 'src/single-race-v5.js',
              ],
              'verified_properties': [
                  'one continuous race',
                  'ten progressive relational levels',
                  'direct cross-device controls',
                  'non-blocking checkpoints',
                  'Mach-class fighter-flight runtime',
                  'byte-for-byte live source identity',
              ],
              'sha256': Path('/tmp/apex-live-checksums.txt').read_text(encoding='utf-8').splitlines(),
              'verified_at': datetime.now(timezone.utc).isoformat(),
          }
          Path('DEPLOYMENT_STATUS.json').write_text(json.dumps(receipt, indent=2) + '\n', encoding='utf-8')
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add DEPLOYMENT_STATUS.json
          git diff --cached --quiet && exit 0
          git commit -m 'Record verified live Apex One-Race v5 deployment [skip ci]'
          git pull --rebase origin main
          git push origin HEAD:main
